Flask==2.3.3
Flask-Limiter
# Use a Flask-SQLAlchemy 3.x release and SQLAlchemy 1.4/2.x compatible range.
# We prefer a flexible range to avoid pinning to a potentially incompatible
# future major release while keeping the dependency resolver free to pick a
# working set for the target environment.
Flask-SQLAlchemy>=3.0,<4
SQLAlchemy>=1.4,<3

